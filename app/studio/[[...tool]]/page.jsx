'use client'

import { Studio } from 'sanity'
import config from '../config.js'

export default function StudioPage({ disableTransition, ...rest }) {
  
  return (
    <>
  <body>
  </body>
    <Studio config={config} />
  </>


  )
}